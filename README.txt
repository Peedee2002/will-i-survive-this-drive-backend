setup:
`pip install requests`
`pip install flask`
`pip install -U googlemaps` 
`pip install json`

then run `python3 server.py`. Make sure port 8000 is free.
also get the frontend from https://github.com/RaghavTheMapGuy/ChromeExtension
